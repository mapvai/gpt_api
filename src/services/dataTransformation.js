// Data transformation module
const dataTransformation = {
  transformData(data) {
    // do any necessary data transformation
	transformedData = data; // For now just return the data as it is
    return transformedData;
  }
};

module.exports = dataTransformation;
